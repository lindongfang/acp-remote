# 干净检出验证（只含被跟踪文件）

- revision: `62ef2649ae6d35e65930df505b2cf41858a19d26`（`git rev-parse HEAD`）
- 取树方式: `git archive 62ef2649ae6d35e65930df505b2cf41858a19d26 -o <tmp>/tree.tar && tar -xf <tmp>/tree.tar -C <tmp>`
- 树内是否含 `.omp/`（未跟踪宿主目录）: NO
- 树内 `.md` 文件数: 88（`git ls-tree -r --name-only 62ef264` 中以 `.md` 结尾的被跟踪文件数；工作区版本因为含宿主未跟踪文件为 104，见 `reports/verify-du1-fixes-2.log`）
- 命令: `node scripts/check-doc-links.mjs`，cwd = 上述解出的树

```
$ exit=0
doc links OK: 366 relative links, 2275 section refs across 88 markdown files
note: 890 section refs 的归属文档由上下文决定（未在同一子句内指名文档），按设计未判定；设 DOC_LINKS_VERBOSE=1 可列出位置
```

说明：本文件替换早前版本（`601c8ae` 提交里那份在**错误的工作目录**执行、标签与内容不符：它报告 104 个 markdown 文件，而只含被跟踪文件的树是 87 个）。当前版本由 `DU1-R3`/复核发现驱动重跑，命令、cwd、revision 与树内文件数都可在上面核对。