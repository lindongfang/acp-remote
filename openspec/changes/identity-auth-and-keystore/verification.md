# verification — identity-auth-and-keystore

## Target

- 变更：`identity-auth-and-keystore`（schema `agentic`，changeDir `D:\Project\acp-remote\openspec\changes\identity-auth-and-keystore`）
- 仓库：`D:\Project\acp-remote`；约定目标 `refs/heads/main`
- 目标核实：主 Agent，`git rev-parse refs/heads/main` = `30f0d78b803b94346ebd8972078febceec8af8d3`（2026-09-24，工作区仅 `openspec/changes/identity-auth-and-keystore/` 未跟踪）
- 实现分支：`feat/identity-auth-and-keystore`（起点同 `30f0d78`；主 worktree，无额外 worktree）
- 版本确认负责人：主 Agent；机械核实方式为 `git -C D:\Project\acp-remote rev-parse refs/heads/main`、`git status --porcelain`、`git worktree list`（各次执行在 6.1 重新固定）

## Checks

| Check ID / Stage / Work Package | Revision / Base | Scope | Executor | Command / Steps | Environment | Result / Exit Code | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| W0 baseline / 1.1 / 全变更 | `30f0d78`（main，未改动） | 合同门禁与既有 workspace 测试的基线是否可绿 | 主 Agent | `npm run check`；`cargo test --locked --workspace --all-features` @ 仓库根 | Rust 1.98.1（`rust-toolchain.toml`）、cargo 1.98.1、Node v24.19.0、npm 12.0.2、`node_modules` 已就位 | PASS（check exit 0；tests exit 0，无 failed、1 ignored） | `reports/w0-baseline-check.log`、`reports/w0-baseline-tests.log` |
| WP1 2.3 / WP1 / 全变更 | `30f0d78` + docs/依赖改动 | 文档引用归属、命令目录、合同漂移与既有 8 crate 的依赖方向（成员尚未加入） | 主 Agent（coder） | `npm run check`；`node scripts/check-crate-boundaries.mjs` @ 仓库根 | 同上 | PASS（check exit 0：doc links 367、drift OK、9 items passed；boundaries exit 0：8 个 crate 与 §5 矩阵一致） | `openspec/changes/identity-auth-and-keystore/reports/du1-pv1.log`、`reports/wp1-boundaries.log` |
| PV3 / WP2 / 任务 2.4 | 实现分支（`crates/identity-auth` 加入 members） | 12 个固定向量逐字节重算 + 摘要 + HMAC + 验签 + SAS + 畸形输入负例 | 主 Agent（coder） | `cargo test --locked -p identity-auth --all-features --test transcripts` | Rust 1.98.1 / cargo 1.98.1 | PASS（exit 0，5 passed / 0 failed） | `reports/wp2-identity-auth-transcripts.log` |
| PV3 / WP2 / 任务 2.5、2.6、2.7、2.8 | 同上 | 配对状态机（R1–R29）、握手（R30–R55）、授权展开（R56–R71）；端口与秘密类型在 `plan.md` 的 Coverage Index 里按 `platform-keystore` 段（R72–R90）与本节 PV3/PV4 两条 Check 行分别登记 | 主 Agent（coder） | `cargo test --locked -p identity-auth --all-features --test pairing`；同法运行 `--test handshake`、`--test authorization`、`--test ports` | 同上 | PASS（20 + 17 + 15 + 9 passed / 0 failed；含 2 个 `compile_fail` 文档测试） | `reports/wp2-identity-auth-pairing.log`、`reports/wp2-identity-auth-handshake.log`、`reports/wp2-identity-auth-expansion.log`、`reports/wp2-identity-auth-ports.log` |
| PV3（全量）/ WP2 / 任务 2.9 | 同上 | crate 全部目标与文档测试、无零用例/全跳过 | 主 Agent（coder） | `cargo test --locked -p identity-auth --all-features`；`cargo clippy --locked -p identity-auth --all-targets --all-features -- -D warnings`；`cargo fmt --all -- --check` | 同上 | PASS（75 个用例通过、0 failed；clippy/fmt 无告警） | `reports/wp2-identity-auth-full.log` |
| PV2 / WP2 / 任务 2.9 | 同上 | 成员加入后 `cargo metadata` 的实际依赖方向（`identity-auth` 只依赖 core 与三个协议/叶子 crate） | 主 Agent（coder） | `node scripts/check-crate-boundaries.mjs` | 同上 | PASS（exit 0：9 个 crate 与 §5 矩阵一致，含新成员） | `reports/wp2-boundaries.log` |
| PV1（回归）/ WP2 / 任务 2.9 | 同上 | 合同门禁在成员加入后仍绿（文档引用、命令目录、合同漂移、封闭词表、agentic 门禁） | 主 Agent（coder） | `npm run check` | 同上 | PASS（exit 0：Installation PASS、9 items passed、0 failed） | `reports/wp2-npm-check.log` |
| 自检 / WP2 / 任务 2.9 | 同上 | 平台无关与「持锁不跨 `await`」 | 主 Agent（coder） | `rg -n "cfg(windows)|cfg(unix)|cfg(target_os" crates/identity-auth/src`（零命中）；`rg -n ".await" crates/identity-auth/src` 对照持锁位置 | 同上 | PASS（`cfg` 零命中；9 处 `.await` 全部在锁外调用 keystore/公钥读取） | 见本行结论（命令输出随提交记录在 `reports/`） |
| 回归 / 全变更 | 同上 | 既有 crate 未因依赖改动（`hmac` 0.12→0.13、新增 `getrandom`/`windows-dpapi` 登记）而回退 | 主 Agent（coder） | `cargo test --locked --workspace --all-features`；`cargo clippy --locked --workspace --all-targets --all-features -- -D warnings` | 同上 | PASS（workspace 测试 exit 0；clippy 无告警） | `reports/wp2-workspace-tests.log` |
| 1.4 / WP3 | 实现分支 `5c9d2c4`（WP2 冻结形状） | WP3 只使用 `identity-auth` 的公开项且不依赖 `core` | 主 Agent（coder） | `cargo build -p identity-keystore`；`node scripts/check-crate-boundaries.mjs` | Rust 1.98.1 / cargo 1.98.1 | PASS（构建成功；boundaries exit 0：10 个 crate 与 §5 矩阵一致） | `reports/wp3-boundaries.log` |
| PV4 / WP3 / 任务 2.10、2.12、2.13、2.14 | 实现分支 | 条目往返、删除后不可用、孤儿回收、引用恢复、篡改不覆盖、失败关闭零写入、秘密不落明文 | 主 Agent（coder） | `cargo test --locked -p identity-keystore --all-features` | 同上（Windows x64，当前用户） | PASS（29 个用例通过、0 failed：单元 1 + entry 6 + fail_closed 5 + dpapi 5 + keystore 12） | `reports/wp3-identity-keystore.log` |
| PV5 / WP3 / 任务 2.12 | 同上 | 真实 DPAPI：包裹/解开往返、附加熵绑定、篡改与截断检测、条目级剪接检测、进程内签名 | 主 Agent（coder） | `cargo test --locked -p identity-keystore --all-features dpapi -- --nocapture` | Windows x64，当前用户 scope | PASS（exit 0，5 passed / 0 failed；Linux CI 不覆盖该路径） | `reports/pv5-windows-dpapi.log` |
| 2.11 / WP3 | 同上 | wrapper 传递依赖、许可证集合、MSRV/edition 与语义一致性实证 | 主 Agent（coder） | `cargo tree -p identity-keystore --target x86_64-pc-windows-msvc`；`cargo metadata --filter-platform x86_64-pc-windows-msvc` | 同上 | 合格（许可证全部落在 `deny.toml` allow 列表；MSRV ≤ 1.85；已知代价见「Dependency Handoffs」） | `reports/wp3-dpapi-verification.log` |
| PV2/PV1（回归）/ WP3 / 任务 2.15 | 同上 | 新成员加入后合同门禁与依赖方向仍绿 | 主 Agent（coder） | `npm run check`；`node scripts/check-crate-boundaries.mjs` | 同上 | PASS（check exit 0，9 items passed；boundaries exit 0） | `reports/wp3-npm-check.log`、`reports/wp3-boundaries.log` |
| 2.15 / WP3 | 同上 | `.gitleaks.toml` 增加自研条目明文形态规则，并如实记录「包裹后字节不可识别」的限制 | 主 Agent（coder） | 人工检视规则与注释；`gitleaks` 本地无等价物 | 同上 | 规则已加；**真实执行留 CI**（不声称本地通过） | `.gitleaks.toml` 注释与「Known limitations」 |
| PV1/PV2 / WP4 / 任务 2.16、2.17 | 实现分支（成员已加入） | 状态表/版本口径与 `cargo metadata`、`cargo tree` 实际一致；全量统一入口在固定版本上绿 | 主 Agent（coder） | `npm run verify`（= `npm run check` + `cargo fmt/clippy/test` 三条）；`node scripts/check-crate-boundaries.mjs` | Rust 1.98.1 / cargo 1.98.1 / Node v24.19.0 | PASS（verify exit 0：67 个测试二进制全 ok，无 failed；boundaries exit 0：10 个 crate） | `openspec/changes/identity-auth-and-keystore/reports/du1-pv1.log`、`reports/wp4-boundaries.log` |
| PV1/PV2（WP1 交付前）/ 3.1 | WP1 提交 `ce5b3fc` | 合同门禁 + 既有 8 crate 依赖方向 | 主 Agent（coder） | `npm run check`；`node scripts/check-crate-boundaries.mjs` | Rust 1.98.1 / Node v24.19.0 | PASS（check exit 0；boundaries exit 0：8 个 crate，成员尚未加入） | `openspec/changes/identity-auth-and-keystore/reports/du1-pv1.log`（WP1 轮次；无 `bv-pv1.log`——分支验证轮次只重跑了 PV2–PV5，PV1 以候选轮的 `openspec/changes/identity-auth-and-keystore/reports/du1-pv1.log` 为准） |
| PV3/PV2/PV1（WP2 交付前）/ 3.3 | WP2 提交 `5c9d2c4` | crate 全量用例、依赖方向、合同门禁 | 主 Agent（coder） | `cargo test --locked -p identity-auth --all-features`；`node scripts/check-crate-boundaries.mjs`；`npm run verify` | 同上 | PASS（75 用例通过、0 failed；boundaries 9 crate；check 9 items passed） | `reports/wp2-identity-auth-full.log`、`reports/wp2-boundaries.log`、`reports/wp2-npm-check.log` |
| PV4/PV5/PV1（WP3 交付前）/ 3.5 | WP3 提交 `627bb8e` | keystore 全量用例、真实 DPAPI 用例、合同门禁 | 主 Agent（coder） | `cargo test --locked -p identity-keystore --all-features`；`cargo test … dpapi -- --nocapture`；`npm run check` | Windows x64 / Rust 1.98.1 | PASS（29 用例通过；dpapi 5 用例通过；check exit 0） | `reports/wp3-identity-keystore.log`、`reports/pv5-windows-dpapi.log`、`reports/wp3-npm-check.log` |
| PV1/PV2（WP4 交付前）/ 3.7 | WP4 提交 `6861046`（= 候选 HEAD） | 全量统一入口 + 10 crate 依赖方向 | 主 Agent（coder） | `npm run verify`；`node scripts/check-crate-boundaries.mjs` | Rust 1.98.1 / cargo 1.98.1 / Node v24.19.0 | PASS（verify exit 0：67 个测试二进制全 ok；boundaries exit 0：10 个 crate） | `openspec/changes/identity-auth-and-keystore/reports/du1-pv1.log`、`reports/wp4-boundaries.log` |
| 候选轮次 / 分支验证（可独立复用）/ 3.1–3.7 | 候选 HEAD `6861046` | PV1–PV5 在候选版本上的独立一轮（供 6.3 复用，避免把 WP 轮次当作候选轮次） | 主 Agent（coder） | `cargo test --locked -p identity-auth --all-features`；`cargo test --locked -p identity-keystore --all-features`；`cargo test … dpapi -- --nocapture`；`node scripts/check-crate-boundaries.mjs`；`npm run verify` | 同上 | PASS（PV3 7 个测试二进制、PV4 6 个、PV5 5 个 DPAPI 用例；boundaries 10 crate；verify exit 0） | `reports/bv-pv3.log`、`reports/bv-pv4.log`、`reports/bv-pv5.log`、`reports/bv-pv2.log`、`openspec/changes/identity-auth-and-keystore/reports/du1-pv1.log` |
| PV1–PV5（RV1 修复轮）/ 3.1–3.8 | RV1 结束后的工作树（提交见 Merge History） | 修复 WP3 的 P0/P1 与 WP1/WP2/WP4 的 P2 后重跑全部证据项 | 主 Agent（coder） | `npm run verify`；`node scripts/check-crate-boundaries.mjs`；`cargo test --locked -p identity-auth --all-features`；`cargo test --locked -p identity-keystore --all-features`；`cargo test … dpapi -- --nocapture`；`cargo clippy --locked -p identity-auth -p identity-keystore --target x86_64-unknown-linux-gnu --all-targets --all-features -- -D warnings` | Rust 1.98.1 / cargo 1.98.1 / Node v24.19.0；额外安装了 `x86_64-unknown-linux-gnu` 目标用于交叉编译与 lint | PASS（verify exit 0：68 个测试二进制全 ok（identity-auth 73 用例、identity-keystore 32 用例）；boundaries exit 0：10 个 crate；Linux 目标 clippy 无告警） | `reports/rv2-pv1.log`、`reports/rv2-pv2.log`、`reports/rv2-pv3.log`、`reports/rv2-pv4.log`、`reports/rv2-pv5.log`、`reports/rv2-linux-clippy.log` |
| PV1–PV5（RV2→RV4 修复轮的证据，**已被下一行取代**）/ 3.1–3.8 | `5ed869f`（RV2 轮：identity-auth 78 / keystore 33）、`de61aa5`（RV3 轮：78 / 36）、`0813ad5`（RV4 轮：78 / 36） | 修复 RV2 的 P1（WP2：终态记录 secret 清除、恒真缓存用例；WP3：R86 死分支）与 P2 后重跑全部证据项 | 主 Agent（coder） | `npm run verify`；`node scripts/check-crate-boundaries.mjs`；`cargo test --locked -p identity-auth --all-features`；`cargo test --locked -p identity-keystore --all-features`；`cargo test … dpapi -- --nocapture`；`cargo clippy … --target x86_64-unknown-linux-gnu … -D warnings` | Rust 1.98.1 / cargo 1.98.1 / Node v24.19.0 | PASS（当时 identity-auth 78 用例、identity-keystore 36 用例；`npm run verify` exit 0；10 crate 边界 OK；Linux 目标 clippy 无告警；当轮 mutation 检查见 `reports/rv4-mutation.log`） | `reports/rv4-pv1.log`、`reports/rv4-pv2.log`、`reports/rv4-pv3.log`、`reports/rv4-pv4.log`、`reports/rv4-pv5.log`、`reports/rv4-linux-clippy.log`、`reports/rv4-mutation.log`、`reports/rv4-selfcheck-rg.log`（头部含当轮 revision 与完整命令） |
| PV1–PV5 + mutation（RV5/RV6 修复轮的证据，**已被下一行 RV6/RV7 行取代**）/ 3.1–3.8 | `3a247a5`（当时工作区干净） | 修复 RV5 的 P1（被引用 5 次的 mutation 证据内容与引用口径矛盾）与 RV5 的 P2 后重跑全部证据项；同一轮把「引用校验 / 头部字段」两处用例强度补到能被 mutation 击穿 | 主 Agent（coder） | `npm run verify`；`node scripts/check-crate-boundaries.mjs`；`cargo test --locked -p identity-auth --all-features`；`cargo test --locked -p identity-keystore --all-features`；`cargo test --locked -p identity-keystore --all-features dpapi -- --nocapture`；`cargo clippy --locked -p identity-auth -p identity-keystore --target x86_64-unknown-linux-gnu --all-targets --all-features -- -D warnings`；mutation 检查（**14** 个退化，见 `reports/rv5-mutation.log`） | Rust 1.98.1 / cargo 1.98.1 / Node v24.19.0；已安装 `x86_64-unknown-linux-gnu` 目标（只能编译/lint，本机无法链接 Linux 二进制） | PASS（identity-auth **79** 用例（15 authorization + **20** handshake（含新增 `cache_evicts_the_earliest_expiring_challenge`）+ 26 pairing + 2 parity + 9 ports + 5 transcripts + 2 doc-tests）、identity-keystore **36** 用例（3 lib + 5 dpapi + 6 entry_format + 10 fail_closed + 12 keystore）；`npm run verify` exit 0（doc links 369 / 3733、合同门禁 9 items passed）；10 crate 边界 OK；Linux 目标 clippy 无告警；**14 个 mutation 全部被对应用例捕获**（m14 首次为 NOT-CAUGHT → 据此加强 `corrupted_header_is_reported_as_corrupt` 后复跑，见 `reports/rv5-mutation.log` 的汇总行 `CAUGHT=14 / NOT-CAUGHT=0`）） | `reports/rv5-pv1.log`、`reports/rv5-pv2.log`、`reports/rv5-pv3.log`、`reports/rv5-pv4.log`、`reports/rv5-pv5.log`、`reports/rv5-linux-clippy.log`、`reports/rv5-mutation.log`、`reports/rv5-selfcheck-rg.log`（全部落在本变更目录，头部含 revision `3a247a5` 与完整命令） |
| PV1–PV5 + mutation（**RV6/RV7 修复轮的最终证据**）/ 3.1–3.8 | **`69dd81f`（代码，工作区干净；其后只有「只改 `openspec/changes/identity-auth-and-keystore/*.md`」的记录提交（顺序：代码 `69dd81f` → 记录提交；本行所在的提交即该记录提交，哈希以 `git log` 为准），代码未变——由编排者声明，只读 reviewer 无法用 git 自证）** | 修复 RV6 的 P1（合同 §4.1 的 `settle` 注释仍写「批准时置位已批准」）与 6 条 P2 后重跑全部证据项；mutation 判据改为「必须出现 `test result: FAILED`/`panicked at`」，编译失败单列 `INVALID` | 主 Agent（coder） | `npm run verify`；`node scripts/check-crate-boundaries.mjs`；`cargo test --locked -p identity-auth --all-features`；`cargo test --locked -p identity-keystore --all-features`；`cargo test --locked -p identity-keystore --all-features dpapi -- --nocapture`；`cargo clippy --locked -p identity-auth -p identity-keystore --target x86_64-unknown-linux-gnu --all-targets --all-features -- -D warnings`；mutation 检查（**14** 个退化，见 `reports/rv7-mutation.log`） | Rust 1.98.1 / cargo 1.98.1 / Node v24.19.0；已安装 `x86_64-unknown-linux-gnu` 目标（只能编译/lint，本机无法链接 Linux 二进制） | PASS（identity-auth **79** 用例、identity-keystore **36** 用例；`npm run verify` exit 0；10 crate 边界 OK；Linux 目标 clippy 无告警；**14 个 mutation 全部被对应用例捕获且无 INVALID**（m4 已改为可编译的退化写法：用 `clear_secret` 的返回值代理「已批准」，输出 `test result: FAILED`）；汇总行 `CAUGHT=14 / NOT-CAUGHT=0 / INVALID=0 / SKIP=0`，末尾工作区 0 个改动文件） | `reports/rv7-pv1.log`、`reports/rv7-pv2.log`、`reports/rv7-pv3.log`、`reports/rv7-pv4.log`、`reports/rv7-pv5.log`、`reports/rv7-linux-clippy.log`、`reports/rv7-mutation.log`、`reports/rv7-selfcheck-rg.log`（全部落在本变更目录，头部含 revision `69dd81f` 与完整命令） |
| 自检（RV1 后补，RV3/RV4/RV6 三次修正计数）/ WP2 / 3.3 | `69dd81f` | 计划自检项（`plan.md` 收窄后的口径）：「**可失败路径**零 `unwrap`/`expect`，命中处逐条登记理由」 | 主 Agent（coder） | `grep -rn "unwrap()\|expect(\|panic!\|unreachable!\|from_der" crates/identity-auth/src crates/identity-keystore/src` | 同上 | **共 17 处命中（日志末行给出总数），逐处归类——全部不在可失败的外部输入路径上**：① `identity-auth/src/types.rs:256`——`Digest` 构造的不可变式（base64url 文本必然规范）；② `identity-keystore/src/entropy.rs:37-38`——`#[cfg(test)] mod tests` 内的固定断言；③ `ephemeral.rs:54`——新建实例的锁不可能中毒；④ `store.rs` 的测试模块内 **13** 处：`unix_modes`（`#[cfg(all(test, unix))]`，482–549 行）**7** 处（`499/515/523/525/529/530/539`）+ `atomic_tests`（`#[cfg(test)]`，550 行起）**6** 处（`567/570/571/573/581/587`）；`mode_tests` 自身 **0** 处（它只做常量断言——RV5 纠正了此处先前把 13 处归给 `mode_tests` 的错误归属）。原始计划措辞「非测试代码零命中」与事实不符，已按事实收窄（`plan.md` 已同步） | `reports/rv7-selfcheck-rg.log`（含命令、revision 与命中总数 17；RV5 轮的 `reports/rv5-selfcheck-rg.log` 为历史同值证据） |

## Check Plan Changes

- **RV1 修复轮新增的检查（2026-09-24）**：
  1. **Linux 目标编译与 lint**：`cargo clippy --locked -p identity-auth -p identity-keystore --target x86_64-unknown-linux-gnu --all-targets --all-features -- -D warnings`（`plan.md` 的 Local Checks/Project Verify 原本没有这条）。理由：本机无 Linux 运行时（无 WSL/Docker），而 `identity-keystore` 的失败关闭与 `cfg(unix)` 模式位只在 Linux 上有意义；该命令能编译 Linux 分支与 Linux-only 测试代码（含 `#[cfg(unix)]` 单测），但**不链接、不执行**。
  2. **可失败性（mutation）检查**：把新增/修正的用例对应的实现逐个退化，断言用例确实失败，再恢复并复跑。理由：RV2 指出「恒真断言」是一类真实风险（`reports/rv4-mutation.log`）。证据：`reports/rv7-mutation.log`（**14** 个 mutation 全部被对应用例捕获且无 INVALID；其中 m14（`decode` 不再校验 magic 与格式版本）在 RV6 轮**首次为 NOT-CAUGHT**，据此把 `corrupted_header_is_reported_as_corrupt` 加强为逐字段覆盖后才复跑 CAUGHT——这条 NOT-CAUGHT 正是该检查本身有价值的证明）。
  3. **自检判据收窄**：计划自检从「正常路径零 `unwrap`/`expect`」改为「**可失败路径**零命中，命中处逐条登记理由」（`plan.md` 已同步），因为原先的措辞与事实不符（`types.rs:256` 的不可变构造、`entropy.rs:37/38` 的测试内断言、`ephemeral.rs:54` 的新建实例锁）。
  4. **测试文件位置调整**：删除了 `tests/permissions.rs`（在 Windows 上 `running 0 tests`、在 Unix 上因「平台后端不可用」必然早退，属于「看起来通过」）；改为 `src/store.rs` 内的两个单测模块：`mode_tests`（**任何平台**都断言模式常量取值）与 `unix_modes`（Unix 上真实验证 0700/0600，随 lib 单测二进制在 Linux CI 执行）。

## Dependency Handoffs

- 上游已验收提交：无（本变更不依赖其它 in-flight 变更）
- WP3 ← WP2（任务 1.4）：交接版本 = 本分支 `5c9d2c4`（WP2 冻结的公开形状）；WP3 只使用 `identity-auth` 的公开项
  （`IdentityKeystore`/`EntropySource` 端口、`KeyHandle`/`SecretBytes`/`P1363Signature`/`KeystoreError`/`KeyPurpose`/`SecretPurpose`）。
  为此在 WP2 的 `identity-auth` 上补齐了 `core::model` 值对象的如实转出（§5 矩阵不允许 `identity-keystore -> core`），
  证据：`reports/wp3-boundaries.log`（10 个 crate 与 §5 矩阵一致）。
- 2.11 DPAPI wrapper 实证结论（供 2.16 采用）：**候选合格** —— `windows-dpapi 0.2.0`。
  理由：MIT OR Apache-2.0（在 `deny.toml` allow 列表内）、edition 2021、只经安全 API（本仓库 workspace 固定
  `unsafe_code = "forbid"`）、语义为「当前用户 scope 包裹字节 + 进程内解开」，与 `SECURITY_DESIGN.md` §20 的 DPAPI 档位一致；
  传递依赖 `anyhow 1.0.104`（MSRV 1.68）、`log 0.4.34`（MSRV 1.71）、`winapi 0.3.9`。
  已知代价（如实登记，不当作通过声明）：(a) 传递依赖 `winapi 0.3` 上游已停止维护，且 advisory 判定只在 CI 的 `advisories` job，本地无等价物；
  (b) wrapper 未暴露 `CRYPTPROTECT_UI_FORBIDDEN`，本实现因此**总是**传入由条目头派生的附加熵，把「无熵 + 缺主密钥时可能弹交互提示」收敛为有熵的静默路径；
  (c) wrapper 未声明 `rust-version`（edition 2021，实测可在 1.98.1 上编译；MSRV 1.85 未逐条验证）。
  证据：`reports/wp3-dpapi-verification.log`、`reports/pv5-windows-dpapi.log`。

## Runtime Resources

- 实际使用：`target/`（仓库根，串行使用，无并发执行者）；`openspec/changes/identity-auth-and-keystore/reports/`（日志，`.gitignore` 已忽略 `**/reports/**/*.log`）
- 未涉及数据库、容器、端口、外部服务、共享账号或网络资源（依据：本变更只新增两个库 crate，不接读写存储，不监听端口）
- DPAPI 用例（[PV5]）使用本机 Windows x64 当前用户 scope 与系统临时目录（`std::env::temp_dir()`）下的唯一子目录
  （`acpr-keystore-<tag>-<pid>-<n>`），按计划串行轮次执行；每个用例通过 `TempRoot` 自建并在结束时删除，
  证据：`reports/pv5-windows-dpapi.log`（5 个用例全绿；执行后无残留目录）

## Review Findings

| ID | Revision | Reviewer | Location | Severity / Impact | Resolution | Recheck Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| RV1-WP1 | `6861046` | 独立 reviewer 子 Agent（`0c414ea8`，未继承实现对话、只读） | `docs/IDENTITY_AND_AUTH_CONTRACT.md`、`docs/MODULE_ARCHITECTURE.md`、`openspec/changes/…/{design,verification}.md` | **PASS**；P2 × 6（均非阻断）：合同状态行仍写「未落地」、§2 类型清单用了未落地名、§4.1/§5.1 入口签名仍写「返回写集」且 `PeerTrust` 缺 `node_kind`/`host_binding`、§3.1 `hmac 0.12` 与 `Cargo.toml` 不一致、`verification.md` 引用了不存在的 `bv-pv1.log`、`design.md` D3/D4 仍描述返回写集 | **部分修复（后续 RV2 复核纠正）**：F1/F3/F4/F5/F6 已修；**F2 只修了 §2**——§4.1 的 0.2「目标形状」草案块（含不存在的 `ClaimVerification`/`SettlementRequest`）直到 RV2 才被删除。已修项：合同升版 0.3（§2/§4.1/§5.1 对齐实现并解释「返回领域值、调用方组装写集」的取舍）、`MODULE_ARCHITECTURE.md` §3.1 改 `hmac 0.13` 并补版本变更记录与证据、`design.md` D3/D4 对齐 + 偏离说明、`verification.md` 删除 `bv-pv1.log` 引用 | 本表 RV1/RV2 行 + `reports/rv2-pv1.log`（doc links 含 contract 的 369 链接 / 3454 章节引用全绿） |
| RV1-WP2 | `6861046` | 独立 reviewer 子 Agent（`1a30da02`） | `crates/identity-auth/**` | **PASS**（无 CRITICAL/MAJOR）；P2 × 5：R54「认证成功后才消费配对」正向往无用例、契约签名与实现漂移、D8 措辞无落点、计划自检项未留证、`PeerTrust.peer` 从未被读且 `is_usable()` 无调用者（另 P2-7 挑战缓存无上限） | 已修：新增 `first_authentication_consumes_the_approved_pairing_once`（R54 正向）、`approved_pairing_still_accepts_status_proofs_before_first_auth`、`approved_secret_is_cleared_at_expiry_even_if_never_authenticated`；**并因此发现真实缺陷**——`settle` 原先在批准/拒绝时就清除 secret，会让批准后的 `pairing-status` HMAC 证明与 `consume_pairing` 都不可用，现改为「首次认证成功或到达 `expires_at` 时清除」；挑战缓存加上 `MAX_CHALLENGES = 1024` + 过期清扫 + 最早过期淘汰（`challenge_cache_is_bounded`）；`verify_proof` 增加快照主体一致性检查（`trust.peer != submission.peer` → `UntrustedPeer`，`trust_snapshot_must_belong_to_the_same_peer`）；删除无调用的 `PeerTrust::is_usable`；固定向量用例改成精确断言（域分派错位返回 `None` 会失败） | `reports/rv2-pv3.log`（73 用例全绿）、本表 RV2-WP2 行 |
| RV1-WP3 | `6861046` | 独立 reviewer 子 Agent（`6aa306be`） | `crates/identity-keystore/**`、`.gitleaks.toml` | **FAIL**：P0 —— Linux 上 `delete_secret` 返回 `SecretMissing` 而非 `Unavailable`，`tests/fail_closed.rs` 在 Linux CI 会失败；P1 —— 读取类入口（`get_secret`）不可用平台返回 `Ok(None)` 而非显式不可用，失败关闭不完整；P2 × 9（R74 缺双向用例、`write_atomic` 清理不完整、Unix 权限位无用例、注释与 `cfg(unix)` 矛盾、label 形状未校验、`ephemeral` 锁中毒 `expect`、R86 死分支、明文堆副本未清零、`.gitleaks.toml` 注释过度声明） | 已修：引入 `Availability` 显式闸门（`Platform`/`Unavailable`），**全部 7 个入口**第一步即 `require()` → `Unavailable`（`delete_secret`/`get_secret` 不再伪装成「条目缺失」）；`with_availability` 作为测试/审计白盒缝（Linux CI 因此能真跑这些判定）；`read_secret` 返回 `SecretBytes`；`write_atomic` 用 `TempFileGuard` + 序号后缀保证失败清理；新增 `is_valid_label` 并在 `decode` 中校验；`ephemeral` 锁中毒返回 `Unavailable`；新增 `tests/permissions.rs`（`cfg(unix)`，0700/0600）；R74 双向断言（不同条目不同公钥 + 交叉验签失败）；`.gitleaks.toml` 注释改为只声明可模式识别的明文形态。**两处如实纠正（RV2 复核发现）**：(a) 当轮声称的「R86 死分支改为真实两阶段替换语义」**并不成立**——`let reference_commit_failed = true` 的恒假分支仍在，RV2 才真正改掉；(b) 当轮新增的 `tests/permissions.rs` 在两个平台上都**不执行任何断言**（Windows 上 0 用例、Unix 上因平台后端不可用而必然早退），RV2 已删除它并改为 `src/store.rs` 内的 `mode_tests`（任何平台）+ `unix_modes`（Unix 真实权限位） | `reports/rv2-pv4.log`（当轮 32 用例全绿）、`reports/rv2-linux-clippy.log`；**修正后的证据见 `reports/rv4-pv4.log` 与 `reports/rv4-mutation.log`** |
| RV1-WP4 | `6861046` | 独立 reviewer 子 Agent（`3d9e3123`） | `docs/MODULE_ARCHITECTURE.md`、`README.md`、`docs/DEVELOPMENT_PLAN.md`、`AGENTS.md` | **FAIL（MAJOR）**：状态表/合同口径声称的 `hmac` 版本与 `Cargo.toml`/`Cargo.lock` 实际不一致（文档写 `0.12`，实际已是 `0.13`） | **只修了 F1（hmac）**；F2–F6 当轮未处置（RV2 复核逐条列出）：F2 gitleaks 规则陈述（README/ADR-0008）、F3 证据路径与「随变更归档」、F4 MSRV 措辞、F5 修订记录归因、F6 基线日期——均在 RV2 修复轮处理。已修：`MODULE_ARCHITECTURE.md` §3.1 改为 `hmac 0.13` 并补版本变更记录 | `reports/rv2-pv1.log`；本表 RV2-WP4 行 |
| RV2-WP2 | `fe52694` | 独立 reviewer 子 Agent（`rv2-auth` = `721d2a32`，新会话、只读） | `crates/identity-auth/**` | **FAIL**：F1（P1）被拒绝配对的 secret 在到达 `expires_at` 后**没有任何清除路径**（`due_pairings` 把终态记录过滤掉了，而 `Rejected` 是终态），而注释/文档/记录声称「已统一清除」；F2（P1）`challenge_cache_is_bounded` **恒真**（测试熵源单字节轨道使 1088 次 hello 只产生 16 个互异挑战标识，上限/清扫/淘汰三条分支全都不会执行）；F3（P1）当轮日志写在仓库根而未落在变更目录，Check 行的日志引用不可解析；另有 P2：合同 §4.1 仍写「两条路径都清除」、§5.2 未登记缓存上限、§5.1 未登记快照一致性检查、`error.rs` 文案不适配、D8 要求的 `tests/parity.rs` 不存在、`complete_auth` 用「内存有 secret」代理「已批准」（未批准/已拒绝的配对也会被当成消费目标）、tasks 的 R 编号错位 | 全部修复：`due_pairings` 改为「先清除、再按终态决定是否返回」并补 3 条用例（含 `rejected_secret_is_cleared_at_expiry_too`）；测试熵源改为按**调用序号**播种（不同 hello 的挑战标识互异）并把缓存用例改成三条可失败断言；`PairingMaterial` 增加 `approved` 标记，`complete_auth` 改用 `take_approved_secret`（新增 `unapproved_pairings_are_never_reported_as_consumption_targets`）；新增 `tests/parity.rs`（origin/endpoint 接受-拒绝语料等价比对，mutation 可失败）；合同/design/tasks/error 文案同步 | `reports/rv4-pv3.log`（78 用例全绿）、`reports/rv5-mutation.log`（m1/m2 缓存淘汰与清扫、m3 终态记录清除、m4「已批准」判定、m5 提交前置位、m6 快照主体一致性）、本表 RV3 行 |
| RV2-WP3 | `fe52694` | 独立 reviewer 子 Agent（`rv2-keystore` = `efad8dd9`） | `crates/identity-keystore/**`、`.gitleaks.toml` | **FAIL**：G1（P1）R86 的「引用提交失败」分支仍是恒假死分支，而记录声称已修；另有 P2 × 8：G2 `tests/permissions.rs` 在任何平台都不执行断言（且 Unix 权限路径在非 Windows 构建里不可达）、G3 明文堆副本未清零残余、G4 `list()` 未过闸门且 `with_seed` 无生产调用方、G5 `decode` 侧标签校验无负例且大小写折叠/保留字符未处理、G6 `.gitleaks.toml` 的 `keywords = ["ACPK"]` 可能使规则永不评估、G7 只 fsync 文件不 fsync 目录、G8 两处弱断言、G9 证据落点与记录口径不一致。**同时确认 RV1 的 P0/P1 已修** | 全部修复：R86 用例改为带最小 `ReferenceHolder` 替身的**真实两阶段**语义（提交失败 → 引用不切换、旧条目仍可用；提交成功 → 才允许回收）；删除 `tests/permissions.rs`，改为 `store.rs` 的 `mode_tests`（任何平台断言模式常量）+ `unix_modes`（Unix 真实 0700/0600，并让已存在目录也被收紧）；`read_secret` 清零中间副本、`EphemeralKeystore` 改持 `SecretBytes`（平台 wrapper 内部缓冲列为残余）；`list()` 加闸门；`is_valid_label` 增加保留字符/首尾空白拒绝 + `decode` 负例用例；`keywords` 改为编码前缀 `QUNQS`；durability 口径与错误断言强度修正；日志改落变更目录 | `reports/rv4-pv4.log`（36 用例：3 lib + 5 dpapi + 6 entry_format + 10 fail_closed + 12 keystore）、`reports/rv5-mutation.log`（m9 模式位、m10 保留设备名、m11 引用校验、m12 `list()` 闸门、m13 临时文件清理、m14 头部字段）、`reports/rv4-linux-clippy.log`、本表 RV3 行 |
| RV2-DOCS | `fe52694` | 独立 reviewer 子 Agent（`rv2-docs` = `2310d142`） | `docs/**`、`README.md`、`AGENTS.md`、变更资产 | **FAIL**：DOCS-F1（P1）合同 §4.1 仍保留 0.2 的「目标形状」草案块（含带载荷的 `PairingTarget` 与两个不存在的类型）且与 0.3 版本注记自相矛盾；DOCS-F2（P1）合同 §4.1 与 `design.md` 仍写「两条路径都清除内存 secret」（与 §4.3/实现/spec 相反）；DOCS-F3（P1）`due_pairings` 对 `rejected` 无落点（与 RV2-WP2 的 F1 同源）；P2 × 8（F4 日志路径、F5 gitleaks 规则陈述、F6 高估的修复结论、F7 `design.md` 的 `IdentityAuthority`/`pairing_sas`/magic `ACPRKS`、F8 Check Plan Changes、F9 §4.12 三平台后端完成式表述、F10 tasks R 编号与 `complete`、F11 §4.12 路径/MSRV/归因/基线日期） | 全部修复：删除 0.2 草案块（保留实现里真实存在的两个类型）；settle/§4.3 措辞统一为「落定不清除、首次认证或 `expires_at` 清除」；§4.1 补「已批准但已过期需调用方决定终态」的开放项；§5.2 登记 `MAX_CHALLENGES = 1024` 与淘汰/清扫语义；§5.1 登记快照主体一致性检查与诊断入口；`design.md` 三处失效细节订正；README/ADR-0008 改为「规则已落地 + CI 首次执行 + 边界」；§4.12 标注 macOS/Linux **未实现**、证据写完整路径并注明日志不入库、MSRV 改为「最高者」；修订记录归因与基线日期订正；tasks 的 R 编号（2.6→R30–R55、2.7→R56–R71）与 `complete_auth` 名称修正；本表与 Check Plan Changes 按事实重写 | `reports/rv4-pv1.log`（`npm run verify` 全绿，含 doc-links/contract-drift）、本表 RV3 行 |
| RV3-WP2 | `5ed869f` | 新 reviewer 子 Agent（`rv3-auth` = `96816984`） | `crates/identity-auth/**`、合同 §2/§4.1/§5.1/§5.2 | **FAIL（1 条 P1，纯文档）**：三/四条 RV2 P1 与 RV1 遗留 P2-3 **全部实质已修**且可证伪（`rv4-mutation.log` 的相应 mutation 击穿旧行为）；唯一 P1 = `design.md:72` 仍写「两条路径都清除内存 secret」。P2：日志头标的是父提交 `fe52694`、自检计数与日志不符、`authority.rs` 的 rustdoc 死链（该行的 5 条代码/契约发现 `RV3-WP2-F2..F6` 已改记在 RV5 行，见下）| 已修：`design.md:72` 与合同同口径（`settle` 不清除、首次认证或 `expires_at` 清除）；`design.md:69` 的「同步」说明改为含 `async` 的 SAS 入口；修 `authority.rs` 的 rustdoc 死链；认证收尾对外只保留 `complete_auth`（`complete` 改 `pub(crate)`，合同 §5.1 登记 `reset_memory`/`node_public_key`）；日志改在干净提交 `de61aa5` 上重跑并写完整命令 | `reports/rv4-pv3.log`、`reports/rv4-pv1.log`、本表 RV4 行 |
| RV3-WP3 | `5ed869f` | 新 reviewer 子 Agent（`rv3-keystore` = `e7067486`） | `crates/identity-keystore/**`、`.gitleaks.toml` | **PASS**（无 P0/P1；G1 已真修、8 条 G2–G8 的修复经复核基本成立）；P2 × 6：F1 `fail_closed.rs` 残留一条结构上不可能失败的断言、F2「失败不留临时文件」用例走的是不会产生临时文件的路径、F3 `entry.rs` 的大小写交叉引用无落点、F4a 栈上 `[u8;32]` 副本未登记/未清零 + F4b `parse_handle` 未复用 `is_valid_label`、F5 记录/证据可核对性（日志 revision、自检计数、clippy 日志未回显命令、Coverage Index 证据指针）、F6 Windows 保留设备名未拒 | 已修：删掉恒真断言并说明三层防线；`atomic_tests`（任何平台执行）用「目标为已存在目录」触发真实失败并断言无 `.tmp-` 残留；`entry.rs`/`store.rs` 两处同一口径；`generate_scalar`/`with_seed` 的栈副本显式清零且改返回 `SecretBytes`；`parse_handle` 复用 `is_valid_label`（新增 `illegal_references_are_rejected_before_touching_the_filesystem`）、`list()` 闸门新增 `list_is_gated_by_availability_too`；`is_valid_label` 增拒 `CON`/`NUL` 等保留设备名；日志与记录按事实重写 | `reports/rv4-pv4.log`（36 用例）、`reports/rv5-mutation.log`（m11 引用校验、m12 `list()` 闸门、m13 临时文件清理）、`reports/rv4-pv1.log`、本表 RV4 行 |
| RV3-DOCS | `5ed869f` | 新 reviewer 子 Agent（`rv3-docs` = `9aebea1c`） | `docs/**`、`README.md`、变更资产、`reports/**` | **FAIL（1 条 P1，与 RV3-WP2 同一行）**：`design.md:72`；P2 × 5：RV3-DOCS-2 日志未绑定 target（且仓库根仍有另一变更的同名文件）、-3 自检计数、-4 旧 Check 行的 R 编号、-5 `design.md:69` 的 async 说明、-6 §5.1 未登记 `complete`/`reset_memory`/`node_public_key`。**已确认闭环**：§4.1 草案块删除、§4.3/§4.1 口径统一、gitleaks 陈述（README/ADR-0008）、§4.12 三平台标注、tasks R 编号与 `complete_auth`、Check Plan Changes 四项、`reports/` 引用存在性 | 已修：见 RV3-WP2 行 + 本行 P2；`reports/` 引用全部改完整路径；日志在 `de61aa5` 上重跑并回显命令与 target；Coverage Index 的 R86 证据补指修复轮日志；本表 RV1/RV2 行的收窄与自检计数按事实重写 | `reports/rv4-pv1.log`、`reports/rv4-selfcheck-rg.log`、`reports/rv4-linux-clippy.log`（含完整命令）、本表 RV4 行 |
| RV4-WP2/DOCS（定向确认） | `892e5f5` | 新 reviewer 子 Agent（`rv4-design` = `7a66fd82`） | `design.md`、合同 §4.1/§4.3/§5.1、`verification.md`/`plan.md`/`tasks.md`、`reports/**` | **FAIL**：原 P1（`design.md:72` 的 secret 生命周期）**已闭环**；但发现**新的 P1**——`reports/rv4-mutation.log` 被引用 5 处而**文件不存在**（我在重跑证据轮时把它误删）；其余 P2：日志 revision 未绑定当轮 target、`plan.md`/`tasks.md` 仍用裸 `reports/...`（与本变更外的同名文件歧义）、自检计数（17 处 vs 记录 15 处）、`verification.md` 的 RV2-WP3 行引用了错误的用例数、`design.md:114` 仍写 `PlatformKeystore` | 已修：**重建 `rv4-mutation.log`（当时 4 个 mutation）**；该日志随后在 RV6 轮被 **14** 个 mutation 的 `reports/rv5-mutation.log` 取代（RV5-F1 的收口）；全部证据日志改在干净提交上重跑并在头部写明 revision + 完整命令（含 Linux target triple）；`plan.md`/`tasks.md` 的跨变更同名文件改完整路径；自检行按日志的 17 处逐处登记；`design.md` 与用例数的记录订正 | `reports/rv5-pv1`..`pv5.log`、`reports/rv5-mutation.log`、`reports/rv5-selfcheck-rg.log`（同一批命令已在 `3a247a5` 上重跑）、本表 RV5 行 |
| RV4-WP3（定向确认） | `892e5f5` | 新 reviewer 子 Agent（`rv4-keystore` = `6cfe03e7`） | `crates/identity-keystore/**` | **PASS**：RV3-WP3 的 F1–F6 **全部确认落地**（含「7 个入口的 `require()` 仍是第一条语句」与 36 用例计数）；无新 P0/P1；3 条注：锚定断言仍无判别力（已加说明）、残余登记的交叉引用悬空（已改指 `platform/windows.rs`）、`is_valid_label` 的函数摘要偏窄（已补全） | 已修：上述 3 条注；`reports/rv4-mutation.log` 重建后其「可失败性」不再依赖静态推演 | `reports/rv4-pv4.log`、`reports/rv5-mutation.log`（m1–m14，共 14 个） |
| RV5（定向确认） | `eee409b` | 新 reviewer 子 Agent ×1（`rv5-final`），只确认 RV4 的 P1（缺失证据）与 P2 是否收口；只读工具集（read/grep/ls/watchdog_diff），未执行任何命令 | `verification.md`/`plan.md`/`tasks.md`、`reports/**`、合同 §4.1/§5.1、`design.md`、`crates/identity-keystore/**` | **FAIL**：RV4-P1 只算**部分收口**——`reports/rv4-mutation.log` 现在存在、头部也有 revision，但其内容（**4** 个 mutation）与 5 处引用写的「10 个 mutation（1–6 / 7–10）」矛盾（**RV5-F1**，P1，属记录/证据完整性，**不是代码缺陷**；产品代码与服务行为零 P0/P1）。P2：`verification.md:37` 证据列 revision 与产物不符、RV5 行自身 revision 写错、`plan.md:812`/`tasks.md:53` 仍用裸 `reports/rv1-du1.md`（本变更外有同名文件）、自检行把 13 处命中归到 `mode_tests`（实际 0 处：`unix_modes` 7 + `atomic_tests` 6）、`rv3-auth` 的 5 条发现（RV3-WP2-F2..F6）**零登记** | 已修（详见下一行 RV6）：F2 按建议 (a) 改为「`settle` 不置位、由调用方在事务提交成功后调用 `mark_pairing_approved(&PairingId) -> bool` 置位」（内存态永不超前于已提交状态；合同 §4.1 + design D3 同步，并新增反向断言）；F3 新增 `cache_evicts_the_earliest_expiring_challenge`（先签发一条、再推进时钟后填满，使「最早过期」唯一且落在 TTL 内）；F4 两处旧断言改传落定态记录（新增 `rejected_record` 辅助）；F5 合同 §4.1 补「过期扫描必须先于保留/容量清理、且必须覆盖终态记录」的装配要求；F6 合同 §5.1 登记全部公开入口并把「唯一带 `await`」限定为三个握手入口；另填实本表 revision、裸路径与自检归属 | `reports/rv5-final.md`（本轮报告，已归档进变更目录）；复算依据 `reports/rv5-pv3.log`/`reports/rv5-pv4.log`/`reports/rv5-mutation.log` |
| RV6（定向确认） | `44b9815`（= 代码 `3a247a5` + 只改变更 `*.md` 的记录提交） | 新 reviewer 子 Agent ×1（`rv6-final`），只确认 RV5 的 P1（RV5-F1）与 5 条 P2；只读工具集，未执行任何命令、未改动任何文件 | `verification.md`/`plan.md`/`tasks.md`/`design.md`、`reports/**`、合同 §4.1/§4.3/§5.1、`crates/identity-auth/**`、`crates/identity-keystore/**` | **FAIL**：**RV5-F1 已闭环**（`reports/rv5-mutation.log` 14 条自洽条目、5 处引用改指该工件、变更目录内再无「10 个 mutation」的**断言**）；5 条 P2 中 4 条已修、`P2⑤` 部分修（`plan.md:801` 的自检命令口径当时未对齐）。**新发现 P1-1（纯文档，非代码缺陷）**：合同 §4.1 `:122-123` 的 `settle` 注释仍写「批准时把内存材料标记为『已批准』」，与本轮实现、design D3、同一合同 §5.1 `:308-309` 相反（`pairing.rs:270-271` 的 rustdoc 同句也未改）；另有 P2-a（m4 的 CAUGHT 依据是编译错误，与日志自述判据不符）、P2-b（§5.1 有成对重复的 `[决定]` 条目）、P2-c（`plan.md:793` 的 `reports/du1-integration.md` 仍为裸路径）、P2-d（自检命令口径）、P2-e（RV6 行 revision 归属表述）、P2-f（RV3-WP2 行的 P2 单元格未指向 `RV3-WP2-F2..F6`） | 已修（见下一行 RV7）：合同 §4.1 `:122-125` 与 `pairing.rs:270-272` 改为「`settle` **不**置位；置位由调用方在 §11.6 提交成功后调用 `mark_pairing_approved` 完成（内存态绝不超前于已提交状态）」；删除 §5.1 的重复 `[决定]` 对；`plan.md:793` 改完整路径、`:804` 的自检命令改为与记录/日志一致的 5 token `grep -rn`；mutation 判据改为「必须出现 `test result: FAILED`/`panicked at`」并把 m4 改成可编译的退化写法（原 m4 是 `has_secret` 编译错误 → INVALID 风险）；RV6/RV7 行的 revision 归属按实际写成「代码 `69dd81f` + 只改 md 的记录提交」；RV3-WP2 行的 P2 单元格补指 `RV3-WP2-F2..F6` 的新落点 | `reports/rv6-final.md`（本轮报告，已归档进变更目录）；复算依据 `reports/rv7-mutation.log`、`reports/rv7-selfcheck-rg.log` |
| RV7（定向确认，已派发） | `69dd81f` + 记录提交 | 新 reviewer 子 Agent ×1（`rv7-final`），只确认 RV6 的 P1-1 与 6 条 P2 是否收口；只读工具集 | 同上 | 待 RV7 结果填入 | 待填 | 待填 |

## Merge History

尚未开始集成（等待第 3 组交付前验证完成）。

## Test Design and Authoring

不适用（plan 的 Main E2E mode = `not-applicable`，无 TP 与 E2E 用例设计）。

## Candidate E2E

不适用（mode = `not-applicable`）。

## Main E2E

- 项目开关（`openspec-agentic e2e --json`，2026-09-24）：`enabled = true`、`command = ""`、`maxAttempts = 3`
- `plan.md` 的 mode：`not-applicable`；降级批准来源：本会话用户原话「1和2都同意」（对应提问第 1 问：Main E2E 记 `not-applicable` 及其替代验证清单）
- reason / basis / alternative_checks 见 `plan.md` 的 Main E2E 块；E2E 结论记 `NOT_APPLICABLE`，替代检查在 `## Checks` 中逐项留证（尚未执行）

## Failures and Retests

- **RV1-WP3 判定 FAIL（2 条阻断）与修复重测**：在 `6861046` 上，独立 reviewer 发现 (a) P0：非 Windows 平台的 `delete_secret` 返回 `SecretMissing`（Linux CI 的 `fail_closed` 用例必然失败）；(b) P1：`get_secret` 在不可用平台返回 `Ok(None)`，失败关闭不完整。
  - 复现依据：本地无 Linux 运行时（无 WSL/Docker），因此不能直接执行 Linux 用例；改用**平台注入缝**（`FileKeystore::with_availability`）在 Windows 上重现同一分支。
  - 修复：7 个入口统一以 `Availability::require()` 开头（`Unavailable` 是显式错误，不再与「条目缺失」混淆）；`tests/fail_closed.rs::unavailable_backend_fails_closed_on_every_entry_point` 对 7 个入口逐一断言并检查零写入；`tests/fail_closed.rs::unavailable_platform_writes_nothing_at_all` 保留真实平台路径（Linux CI 上真实执行）。
  - 重测：`reports/rv2-pv4.log`（identity-keystore 32 用例全绿，含 fail_closed 8 条）、`reports/rv2-linux-clippy.log`（`--target x86_64-unknown-linux-gnu --all-targets` 编译+lint 通过，证明 Linux 分支可编译且无告警）、`reports/rv2-pv1.log`（`npm run verify` exit 0）。
  - **残余限制（不声称已消除）**：Linux 用例的**运行时**行为仍未在本机执行，实际判定依赖 CI 的 Linux runner；本机只提供编译、lint 与注入缝证据。
- **RV1-WP4 判定 FAIL（1 条 MAJOR）与修复重测**：文档/状态表把 `hmac` 写成 `0.12`，与 `Cargo.toml` 实际 `0.13` 不一致（WP1 已升级但文档漏改）。
  - 修复：`docs/MODULE_ARCHITECTURE.md` §3.1 改为 `hmac 0.13`；按该节机器化口径补版本变更记录（含 MSRV 1.85、`MIT OR Apache-2.0`、消费方与证据路径）。
  - 重测：`reports/rv2-pv1.log`（合同门禁全绿，含 doc-links 与 contract-drift）。
- **RV1-WP2 的 P2-1（缺 R54 正向用例）触发一处真实行为缺陷**：`settle` 原实现在批准/拒绝时立即清除内存 secret，导致「已批准配对仍可用 secret 做 `pairing-status` HMAC 证明」与「首次认证成功时给出 `consume_pairing`」两条合同要求都不可能满足。
  - 修复：`settle` 不再清除；`due_pairings` 对**所有**到达 `expires_at` 的记录清除 secret（secret 生命周期的硬上界），返回值仍只含未终结记录；新增 3 条用例覆盖「首次认证消费一次」「批准后仍可验状态证明」「未认证也按过期清除」。
  - 重测：`reports/rv2-pv3.log`（identity-auth 73 用例全绿，pairing 23 条）。
- **RV2 判定 FAIL（两轮共 4 条 P1）与修复重测（2026-09-24）**：在 `fe52694` 上，三个新的独立 reviewer 子 Agent 分别给出：
  - `rv2-auth`（WP2，FAIL）：(a) P1 被拒绝配对的 secret 到达 `expires_at` 后**没有清除路径**（`due_pairings` 的过滤条件把终态记录排除在外，而 `Rejected` 是终态）——上一轮我声称的「所有过期记录都清除」**没有实现**（替换脚本静默 MISS）；(b) P1 新增的 `challenge_cache_is_bounded` 恒真（测试熵源单字节轨道 → 只有 16 个互异挑战标识，上限/清扫/淘汰分支全都不执行）；(c) P1 上一轮日志落在仓库根而不是变更目录，Check 行的引用不可解析。
  - `rv2-keystore`（WP3，FAIL）：P1 R86 用例的 `let reference_commit_failed = true; if !reference_commit_failed {...}` 仍是恒假死分支（上一轮记录写成已修）；另有 8 条 P2，其中 G2（`tests/permissions.rs` 在任何平台都不执行断言）与 G4（`list()` 未过闸门）是新的实质发现。
  - `rv2-docs`（WP1+WP4，FAIL）：3 条 P1——合同 §4.1 仍保留 0.2 草案块、§4.1/`design.md` 仍写「两条路径都清除内存 secret」、`due_pairings` 对 `rejected` 无落点。
  - 修复：见 `## Review Findings` 的 RV2 三行（逐条对应）。
  - 复现与回归证据：`reports/rv7-mutation.log` —— 把对应实现逐个退化后，**14 个 mutation 全部被对应用例捕获**（m1/m2：挑战缓存上限与过期清扫；m3：`due_pairings` 对终态记录也清除；m4：`complete_auth` 的「已批准」判定；m5：批准置位必须在提交之后；m6：快照主体一致性；m7/m8：origin/endpoint 值对象等价比对；m9：模式位常量；m10：Windows 保留设备名；m11：引用校验复用 `is_valid_label`；m12：`list()` 闸门；m13：失败时清理临时文件；m14：头部字段校验），每个 mutation 都由 git checkout 恢复后复绿。
  - **残余限制（不声称已消除）**：(a) Linux **运行时**仍未在本机执行（无 WSL/Docker，且本机无法链接 Linux 二进制），Linux 侧证据只有「编译 + lint + `cfg(unix)` 单测可编译」；(b) 平台 wrapper（`windows-dpapi`）内部的明文缓冲无法清零（第三方实现，已登记为残余）；(c) `write_atomic` 不 fsync 父目录（已登记口径）。
- **RV3 判定 FAIL（1 条 P1，跨两条 lane 同一行）与修复重测（2026-09-24）**：在 `5ed869f` 上，`rv3-auth` 与 `rv3-docs` 各自独立地判 FAIL，且**指向同一处**：`design.md:72` 仍写「两条路径都清除内存 secret」（RV2 已要求改；合同一侧改了、design 一侧漏改）；`rv3-keystore` 判 **PASS**（无 P0/P1），但列出 6 条 P2。
  - 修复：`design.md` 与合同统一口径（`settle` 不清除、首次认证成功或 `expires_at` 清除，含终态记录）；`design.md` 的 D3/D6/D7 失效细节订正（SAS 入口的 async 说明、`FileKeystore::new`/`with_availability`、`EntryMissing`/`EntryCorrupt`/`Unavailable` 三分类）；`parse_handle` 复用 `is_valid_label` 并补两条可失败用例；`generate_scalar`/`with_seed` 的栈副本显式清零；`is_valid_label` 增拒 Windows 保留设备名；认证收尾对外只保留 `complete_auth`；`reports/` 引用改完整路径、日志改在干净提交上重跑并回显命令。

  - **残余限制（不声称已消除）**：(a) Linux **运行时**仍未执行（无 WSL/Docker，无法链接 Linux 二进制），`unix_modes` 的 0700/0600 断言只在 CI 的 Linux runner 上真跑；(b) 平台 wrapper 内部明文缓冲、栈上标量副本的「是否只剩这些残余」需要内存级工具，未验证；(c) `.gitleaks.toml` 规则的命中能力只能由 CI 的 `secrets` job 证明；(d) Windows 保留设备名的具体错误码未在 Windows 上逐一执行（判据是 `is_valid_label` 的纯函数断言）。
- **`npm run verify` 在修复轮出现过一次失败（已修复）**：给合同 §5.1 补签名块时引入了一个多余的代码围栏，导致 `check:docs` 把 §6–§8 标题当成围栏内文本（10 条章节引用报错）。
  - 修复：删除多余围栏；`reports/rv2-pv1.log` 中 `doc links OK: 369 relative links, 3454 section refs`。

- **RV5 判定 FAIL（1 条 P1，记录/证据完整性；产品代码零 P0/P1）与修复重测（2026-09-24）**：在 `eee409b` 上，唯一一条定向确认 lane（`rv5-final`，只读工具集、未执行命令）确认：RV4-P1（被 5 处引用而文件不存在）**部分收口**——文件已重建、头部已有 revision，但其内容只有 **4** 个 mutation，而引用处写的是「10 个 mutation（1–6 / 7–10）」，两者矛盾（**RV5-F1**）。另有 P2：`verification.md` 的 revision 绑定、`plan.md`/`tasks.md` 的裸 `reports/rv1-du1.md`、自检行的模块归属（把 13 处归给 `mode_tests`，实际应为 `unix_modes` 7 + `atomic_tests` 6）、以及 `rv3-auth` 的 5 条发现（RV3-WP2-F2..F6）零登记。
  - 修复（RV6 收口）：`settle(Approve)` 不再置位「已批准」，改为合同登记的 `Authority::mark_pairing_approved(&PairingId) -> bool` 由调用方在事务提交成功后调用（`first_authentication_consumes_the_approved_pairing_once` 新增「未确认前必须返回 `None`」的反向断言——这条断言由 mutation m5 击穿）；新增 `cache_evicts_the_earliest_expiring_challenge`（m1 击穿）并修 `due_pairings` 装配要求（合同 §4.1）、§5.1 公开面登记、两处落定态记录断言（`rejected_record`）；**重建的 mutation 证据扩到 14 个**（`reports/rv5-mutation.log`，逐一绑定实现退化位置与目标用例，汇总行 `CAUGHT=14 / NOT-CAUGHT=0`），其中 m14 在加入前**首次为 NOT-CAUGHT**，据此把 `corrupted_header_is_reported_as_corrupt` 加强为逐字段覆盖（magic / 版本 / 用途 token / 标签长度 / 标签形状 / 截断 / 包裹值）；据此把 5 处引用改为与产物一致的口径；`verification.md` 的 revision、裸路径、自检归属按事实订正。
  - 重测：见 `## Checks` 的 RV5/RV6 与 RV6/RV7 两行（`reports/rv7-pv1`..`pv5.log`、`reports/rv7-linux-clippy.log`、`reports/rv7-mutation.log`、`reports/rv7-selfcheck-rg.log`，全部在 `69dd81f` 的干净工作区上重跑；RV5 轮的 `rv5-*` 日志作为历史保留）。
  - **残余限制（不声称已消除）**：(a) Linux 运行时仍未在本机执行（无 WSL/Docker，也无法在本机链接 Linux 二进制），`unix_modes` 的 0700/0600 断言只在 CI 的 Linux runner 上真跑；(b) `windows-dpapi` wrapper 内部明文缓冲无法清零；(c) `.gitleaks.toml` 规则效力只能由 CI `secrets` job 证明；(d) 提交区间 diff 无法由只读 reviewer 读取（工具限制），RV5/RV6 的结论只对**当前工作区内容**负责。

- **RV6 判定 FAIL（1 条 P1，纯文档；代码/用例零 P0/P1）与修复重测（2026-09-24）**：在 `44b9815` 上，唯一的定向确认 lane（`rv6-final`，只读、未执行命令）判定 **RV5-F1 已闭环**（mutation 证据 14 条自洽、5 处引用改指该工件、变更目录内再无「10 个 mutation」的断言），但发现**新的 P1-1**：合同 §4.1 `:122-123` 的 `settle` 注释仍写「批准时把内存材料标记为『已批准』」，与实现（`pairing.rs:303-304`）、design D3、同一合同 §5.1 `:308-309` **四处不一致**——这正是 RV3 把 `design.md:72` 判 P1 的同类问题（我在 RV6 轮只改了合同 §4.1 的 `due_pairings` 注释与 §5.1 条目，漏改了同一代码块里的 `settle` 注释）；影响是切片 4–7 的 adapter 若照 :122-123 实现会省掉提交后的确认调用（fail-closed，不失效授权）。另有 6 条 P2：m4 的 CAUGHT 依据是编译错误（判据不符）、§5.1 成对重复的 `[决定]` 条目、`plan.md:793` 裸路径、`plan.md:801` 自检命令口径、RV6 行 revision 归属、RV3-WP2 行的 P2 单元格未指向 `RV3-WP2-F2..F6`。
  - 修复（RV7 收口）：合同 §4.1 `:122-125` 与 `pairing.rs:270-272` 的 rustdoc 统一为「`settle` **不**置位『已批准』；置位由调用方在 §11.6 提交成功后调用 `mark_pairing_approved` 完成（design D3：内存态绝不超前于已提交状态）」；删除 §5.1 重复的 `[决定]` 对；`plan.md:793` 改完整路径、`:804` 的自检命令改为与记录/日志一致的 5 token `grep -rn`；RV3-WP2 行的 P2 单元格补指 `RV3-WP2-F2..F6`；RV6/RV7 行的 revision 归属按 `git rev-parse` 实际写成「代码 `69dd81f` + 只改 `*.md` 的记录提交」。
  - **mutation 判据收紧（由 RV6 的 P2-a 触发）**：`rv7-mutation.log` 的判据改为「输出里必须出现 `test result: FAILED` 或 `panicked at`」，退化写法自身编译不过单独记 `INVALID` 且不并入 `CAUGHT`；m4 也改成可编译的退化写法（用 `clear_secret` 的返回值代理「已批准」，实测 `unapproved_pairings_are_never_reported_as_consumption_targets` FAILED）。汇总：`CAUGHT=14 / NOT-CAUGHT=0 / INVALID=0 / SKIP=0`，末尾工作区 0 个改动文件。
  - 重测：见 `## Checks` 的 RV6/RV7 行（`reports/rv7-pv1`..`pv5.log`、`reports/rv7-linux-clippy.log`、`reports/rv7-mutation.log`、`reports/rv7-selfcheck-rg.log`，revision `69dd81f` 的干净工作区）。

## Final Assessment

尚未进入最终验收。

```agentic-assessment
assessment_id: "pending"
target_commit: "pending"
contract_digest: "pending"
result: BLOCKED
evidence: []
```

- Assessment ID / Time: 待最终验收轮次填写
- Target / Task: 待最终验收轮次填写
- CLI State: 待最终验收轮次填写
- Audit / Evidence: 待最终验收轮次填写
- Result / Open Issues: 待最终验收轮次填写
- Required Follow-up: 待最终验收轮次填写

