## 1. Dependency and Resource Setup

- [x] 1.1 全变更；可选 environment/recon 执行者。在**新的任务级最小上下文**中核实：仓库绝对路径与目标引用（`git rev-parse refs/heads/main`、`git status --porcelain`、`git worktree list`）、工具链版本（`rust-toolchain.toml` 与 `cargo --version`）、Node 版本、`npm ci` 状态、既有合同门禁与 `cargo test --locked --workspace --all-features` 的基线结果、以及 `crates/` 与 `openspec/specs/` 的实际现状。完成条件：返回结构化事实（命令、退出码、关键输出）并写入 `verification.md` 的运行时基线；若基线本身为红，如实记录并把影响告知主 Agent，不开始实现。
- [x] 1.2 WP1–WP5；实现 Agent（coder）。确认实现所需契约基线（`docs/LOCAL_ADMIN_PROTOCOL.md` 现行版、`docs/MODULE_ARCHITECTURE.md` §3.1/§4.9/§4.10/§5、`docs/CONFIG_REFERENCE.md`、`docs/SECURITY_DESIGN.md` §9.4/§12.1/§14、`design.md` D1–D9）与文件所有权（WP1–WP5 的写范围与单一写入者，见计划「Work Packages」与「Execution Waves」）。完成条件：记录编码起点与写入范围（计划 WP 表的输入清单 + `git rev-parse HEAD`）；契约明确后即可开始编码，不等待其它轨道。
- [x] 1.3 WP1–WP5；实现 Agent（coder）。为本变更实际需要的运行资源确定隔离方式：每个执行者独立的 `CARGO_TARGET_DIR`、集成用例自建且在结束时删除的临时 Daemon 数据目录、[PV5] 的 Windows IPC 用例串行轮次（计划「Runtime Resources」）。完成条件：记录资源名、隔离/独占方法与释放步骤；确认本变更不涉及数据库服务、容器、端口、外部账号或网络资源，并把该依据写入 `verification.md`。
- [ ] 1.4 WP3/WP4；实现 Agent（coder）。在 [PV3]/[PV4] 集成验证前接入上游已验收形状（WP2 的 wrapper safe API、WP3 的 `DaemonControl`/信封类型/方法路由），核对实际基线提交与包含关系，并引用 `verification.md` 中的交接证据（计划「Dependency Handoffs」的 WP3/WP4 行）。完成条件：`cargo build -p server` 仅经 path 依赖使用 wrapper 公开 API、`cargo build -p app` 只使用 `server` 公开项；上游形状变化时按失效列重开相应证据。

## 2. Implementation

- [x] 2.1 WP1；前置：1.2；实现 Agent（coder）。把 `docs/MODULE_ARCHITECTURE.md` 的本轮范围写到位：§3/§3.1 登记 `server`/`app` 与新依赖口径（tokio `net`/`io-util`/`signal`、nix `socket`/`user`、clap、toml、文件锁 crate、`vendor/windows-local-ipc`）、§4.9/§4.10 标注本轮落地的模块子集（`server` 仅 `transport` 本地通道部分与 `local_admin`；`acp_facade`/`sync`/`node_link` 仍待后续切片）、§5 收敛「缺列」注记（`server`/`app` 开始成为列）；`docs/LOCAL_ADMIN_PROTOCOL.md` §3.1 补 facade 缺席期 `0x02` 即连即关的**实现状态注记**（`design.md` D5，不改语义）。完成条件：文档与 `design.md` 一致，引用关系可被 `check:doc-links` 接受；`docs/CONFIG_REFERENCE.md` 未被改动（本变更不新增键）。
- [x] 2.2 WP1；前置：1.2、2.1；实现 Agent（coder）。在 `Cargo.toml` 的 `[workspace.dependencies]` 登记新依赖与 feature 增量，`workspace.exclude` 登记 `vendor/windows-local-ipc`，`deny.toml` 登记 path 来源；**不**新增 `[workspace] members`（成员由 2.7/2.14 按波次加入）。文件锁 crate 按 `SECURITY_DESIGN.md` §20 在 `fs4`/`fd-lock` 候选中实证后选定并留证。完成条件：`cargo metadata --no-deps` 可解析；依赖登记与 `deny.toml` 的 allow 列表、`[graph] targets` 不冲突；不抬高 `rust-version`。
- [x] 2.3 WP1；前置：1.3、2.2；实现 Agent（coder）。在成员未加入的状态下跑局部验证：`npm run check`（重点 `check:doc-links`、`check:command-catalog`、`check:contract-drift`）与 `node scripts/check-crate-boundaries.mjs`。完成条件：[PV1]/[PV2] 的 W0 轮次通过（此阶段 PV2 只核对既有成员与 `core` 闭包），日志写入 `reports/wp1-boundaries.log` 与 `openspec/changes/daemon-cli-and-local-admin/reports/du1-pv1.log`（W0 轮次可覆盖写入，最终轮次在 6.3/6.7/7.1 重跑）。
- [x] 2.4 WP2；前置：2.2；实现 Agent（coder）。建立 `vendor/windows-local-ipc`（workspace 外的 path crate，不继承 workspace lint）：`cfg(windows)` 下实现两个 safe 函数——「以仅含本用户 SID 的 SDDL 创建 Named Pipe」与「查询已连接 pipe 对端的用户 SID」（`GetNamedPipeClientProcessId` + `OpenProcessToken` + `GetTokenInformation`），unsafe 收敛在模块级并逐处附不变量注释；非 Windows 编译通过且运行期明确失败。完成条件：crate 自身测试通过（Windows 本机覆盖 SDDL 与 SID 查询往返），日志写入 `reports/pv5-windows-ipc.log`。
- [x] 2.5 WP2；前置：2.4；实现 Agent（coder）。补齐 wrapper 的负向与边界用例：非法 SDDL/句柄失败的错误分类、非 Windows 运行期明确失败、公开 API 不暴露原始句柄或可滥用参数。完成条件：用例通过且无零用例；任何 unsafe 不越出该 crate（`rg -n "unsafe" crates/` 零命中）。
- [x] 2.6 WP2；前置：2.4、2.5；实现 Agent（coder）。跑 WP2 交付前局部验证：wrapper 自身 `cargo build/test`（Windows 本机 [PV5] 轮次）与主 workspace 的 `npm run check` 不受影响。完成条件：safe API 形状冻结并作为 handoff 交 WP3；Windows 用例原始日志已保存。
- [x] 2.7 WP3；前置：1.4、2.6；实现 Agent（coder）。建立 `crates/server` 骨架并把该 crate 写入 `[workspace] members`；实现 `transport::local` 的 framing 层（`design.md` D1）：`u32be length | channel | payload` 编解码、1 MiB 上限、空帧/未知 channel/混用 channel/超未完成请求上限（32）一律关闭连接且不返回错误帧、`FacadeAttachmentId` 分配与作废、facade 缺席期 `0x02` 即连即关并记结构化警告（D5）。完成条件：[PV3] 中 framing 与 channel 绑定场景（[R23]–[R32]）通过，日志写入 `reports/wp3-server-transport.log`。
- [x] 2.8 WP3；前置：2.7；实现 Agent（coder）。实现 endpoint 与访问控制（`LOCAL_ADMIN_PROTOCOL.md` §2）：Windows pipe 名规则 + 经 `windows-local-ipc` 的 SDDL 创建与对端 SID 校验，Unix socket 位置/`0700`/`0600`/`XDG_RUNTIME_DIR` 回落 + `SO_PEERCRED` uid 比对（`nix`）；凭据不一致不发任何 frame、立即关闭并记 `authorization.denied` 审计；endpoint 创建失败即失败关闭。完成条件：[PV3] 覆盖 [R17]–[R22]（Unix 路径由 Linux CI 覆盖），Windows 路径本机执行 [PV5] 并写入 `reports/pv5-windows-ipc.log`。
- [x] 2.9 WP3；前置：2.7；实现 Agent（coder）。实现 `local_admin` 管理信封（`design.md` D1/D6）：closed object 编解码、`v=1` 规则、`id`/`method`/`params` 校验与 `local.invalid_request`/`local.invalid_params`/`local.unsupported` 映射、每请求恰好一个响应、`error.message` 脱敏约束；`include_str!` 内嵌 `schemas/local-admin/v1/envelope.schema.json` 做方法集/错误码漂移测试，`fixtures/local-admin/v1/` valid/invalid 信封做往返测试。完成条件：[PV3] 覆盖 [R33]–[R36]，日志写入 `reports/wp3-server-envelope.log`。
- [x] 2.10 WP3；前置：2.9；实现 Agent（coder）。实现本地配置族与 Daemon 生命周期方法路由（`design.md` D6）：`workspace.select`（canonicalize 与路径校验）、`agent.configure`、`provider.configure`（凭据只经 `IdentityKeystore` 端口、`result` 只回字段名、keystore 不可用 → `local.unavailable`）、`daemon.status`/`daemon.stop` 经 `DaemonControl` 注入由组合根回答。完成条件：[PV3] 覆盖 [R37]–[R41] 与 [R8]/[R9]/[R12]/[R14] 的服务端部分，日志写入 `reports/wp3-server-methods.log`。
- [x] 2.11 WP3；前置：2.10；实现 Agent（coder）。实现设备/节点配对方法编排（`design.md` D6）：`device.pair.*` 与 `node.pair.*`（owner 模式）调用组合根持有的 `identity-auth` 配对状态机，领域值经 core 用例组装写集单事务提交；confirm 在持久提交后才返回；revoke 提交后通知组合根关闭 active connection 才返回；`mode = "access"` 与 `node.rotate-key.begin` 返回 `local.unsupported`。完成条件：[PV3] 覆盖 [R42]–[R48]，日志写入 `reports/wp3-server-methods.log`。
- [x] 2.12 WP3；前置：2.10；实现 Agent（coder）。实现 Export/Import 与审计导出方法（`design.md` D6）：`export.create`（首切片恰好 1 项/无参数 template/alias 必须已建立/`local.conflict`）、`export.list`/`export.revoke`、`import.add` 恒 `local.unavailable`、`import.list`/`import.remove`；`audit.export` 经 `AuditStore::query` + jsonl/csv 写出 + sha256，内容只含元数据字段、已存在文件 → `local.conflict`；重试与失败语义（`local.not_found`/`local.conflict` 区分）与审计事件覆盖。完成条件：[PV3] 覆盖 [R49]–[R58]，日志写入 `reports/wp3-server-methods.log`。
- [x] 2.13 WP3；前置：2.7–2.12；实现 Agent（coder）。跑 WP3 交付前局部验证：`cargo fmt --all -- --check`、`cargo clippy --locked -p server --all-targets --all-features -- -D warnings`、`cargo test --locked -p server --all-features`（[PV3]），并自检 `rg -n "unsafe" crates/server/src`（零命中）与方法路由只经 `core::use_cases`。完成条件：[PV3] 全绿且无零用例/全跳过；`DaemonControl`、信封类型与方法路由形状冻结，可供 WP4 装配。
- [ ] 2.14 WP4；前置：1.4、2.13；实现 Agent（coder）。建立 `crates/app` 骨架并把该 crate 写入 `[workspace] members`；实现组合根与 Daemon 生命周期（`design.md` D2/D4/D8）：配置加载（toml）与首次种子导入（`seed_state`/`mark_seeded` 单事务）、单实例锁与 `instanceId`、启动序列、按 `SECURITY_DESIGN.md` §12.1 的关闭序列、周期任务（prune/expire_pairings/sweep_orphans/批量刷盘）的所有者与取消、`DaemonControl` 实现（`counts` 聚合、`links` 恒空）。完成条件：[PV4] 覆盖 [R1]–[R9]、[R12]–[R16]，日志写入 `reports/wp4-app-daemon.log`；endpoint 失败关闭的 Windows 路径随 [PV5] 在本机留证。
- [ ] 2.15 WP4；前置：2.14；实现 Agent（coder）。实现 `app::cli` 的子命令骨架与错误契约（`design.md` D7）：clap 解析 §5.8 全部子命令、kebab-case flags → camelCase params、统一错误出口（成功 0/失败非零 + stderr 一行含 `code` 的结构化 JSON）、本地通道客户端（复用 `server::local_admin` 信封类型）、Daemon 未运行时的进程内回答与明确失败。完成条件：[PV4] 覆盖 [R10]/[R11]、[R59]–[R63]，日志写入 `reports/wp4-app-cli.log`。
- [ ] 2.16 WP4；前置：2.15；实现 Agent（coder）。实现配对仪式与凭据交互（`design.md` D7）：`device pair`/`node pair` 的 begin → 轮询 → confirm 编排、认领后展示名称/指纹/SAS/scopes/过期时间、非交互 `--sas`/`--fingerprint` 逐字校验、无「无参数自动确认」开关、`pairingUrl` 只存在于内存与终端；`provider configure` 的无回显凭据录入与非交互环境明确失败；`agent configure`/`export create` 的 `--file` JSON 与错误码映射。完成条件：[PV4] 覆盖 [R64]–[R69]，日志写入 `reports/wp4-app-cli.log`。
- [ ] 2.17 WP4；前置：2.15；实现 Agent（coder）。实现 `doctor`（在线走 `daemon.status` + 本地检查、离线进程内完成，不新增方法）与 `acp-stdio`（stdin/stdout ↔ `0x02` 字节泵；Daemon 未运行/正在关闭/即连即关时明确错误退出，绝不打开数据库或启动核心）。完成条件：[PV4] 覆盖 [R70]–[R72]（真实子进程集成测试），日志写入 `reports/wp4-app-cli.log`；Windows 路径本机执行并写入 `reports/pv5-windows-ipc.log`。
- [ ] 2.18 WP4；前置：2.14–2.17；实现 Agent（coder）。跑 WP4 交付前局部验证：`cargo fmt --all -- --check`、`cargo clippy --locked -p app --all-targets --all-features -- -D warnings`、`cargo test --locked -p app --all-features`（[PV4]），并自检 `rg -n "rusqlite|sqlx" crates/app/src`（CLI 路径零命中）与 `rg -n "unsafe" crates/app/src`（零命中）。完成条件：[PV4] 全绿且无零用例/全跳过。
- [ ] 2.19 WP5；前置：2.13、2.18；实现 Agent（coder）。把「已落地」标记写回：`docs/MODULE_ARCHITECTURE.md` §5 缺列注记收敛与 §3/§4.9/§4.10 状态，更新 `README.md`「仓库当前状态」、`docs/DEVELOPMENT_PLAN.md` §2、`AGENTS.md` §4 的模块状态表。完成条件：文档陈述与实际 `cargo metadata`/`cargo tree` 一致；不把 `node-link-client`、`server::acp_facade`/`sync`/`node_link` 写成已落地。
- [ ] 2.20 WP5；前置：2.19；实现 Agent（coder）。在成员已加入的状态下跑全量统一入口 `npm run verify`（[PV1]）与 `node scripts/check-crate-boundaries.mjs`（[PV2]）。完成条件：两个检查在固定版本上全绿、日志写入 `openspec/changes/daemon-cli-and-local-admin/reports/du1-pv1.log`；若既有门禁因本变更变红，就地修复后再跑，不以「稍后修」结项。
- [x] 2.21 WP3（增量）；前置：2.13；实现 Agent（coder）。按 2026-09-25 用户裁决（选项 a）把 `node.rotate-key.begin` 纳入 v1 方法词表并**原子交付**：① `schemas/local-admin/v1/envelope.schema.json` 的 `methodName.pattern` 放宽为段内允许连字符（`^[a-z][a-z0-9]*(\.[a-z0-9]+(-[a-z0-9]+)*)*$`）、`enum` 增至 25 项；② `docs/LOCAL_ADMIN_PROTOCOL.md` §4 表格正则与 §8 版本注记同步；③ `fixtures/local-admin/v1/` 新增一条 valid（`node.rotate-key.begin` 请求）与一条 invalid（段首/段尾连字符）并登记 `manifest.json`；④ `scripts/check-command-catalog.mjs` 的方法标题/能力表正则放宽到能捕捉连字符名字（否则 25 vs 24 集合比对必然失败，已核实原正则排除连字符）；⑤ Rust 侧新增 `Method::NodeRotateKeyBegin` 变体与路由分支（返回 `local.unsupported`），同步 `crates/server/tests/local_admin_schema_drift.rs` 的期望集合。完成条件：`npm run check` 全绿、`cargo test --locked -p server --all-features` 全绿且 drift 测试为 25 项逐项相等；`commands.json` 的 `localCapabilities` 未被改动。日志写入 `reports/wp3-server-methods.log`。
- [ ] 2.22 WP4（增量，前置于 2.14）；实现 Agent（coder）。在 `storage-sqlite` 实现 `AuditStore`（合同 `CORE_PORTS_AND_STORAGE.md` §5 的 `append`/`query`）：写包落 `owned_audit`（列与 CHECK 已存在，复用 `insert_audit_rows` 同口径的编码），`query` 按 `AuditQuery`（`since`/`until`/`actions`/`actor`/`target`/`limit`）按 `at` 升序读回并按合同映射回 `AuditRecord`；不新增/修改表结构（DDL 由合同漂移门禁钉死）。写范围：`crates/storage-sqlite/src/admin/audit.rs`、`crates/storage-sqlite/src/admin/mod.rs`。完成条件：该 crate 测试（含 append/query 往返、时间区间与动作/actor/target 过滤、limit、空结果、容量与保留交互）全绿；`npm run check`（含 `check:drift`）全绿；日志写入 `reports/wp4-storage-audit.log`。依据：Check Plan Changes 2026-09-25 新增任务 2.22 条（`UseCaseDeps.audit` 为必需字段、`audit.export` 靠 `query`）。
- [x] 2.23 WP3（增量，补 2.11/2.12 的重试语义）；前置：2.13；实现 Agent（coder）。修正 `*.revoke` 的重试语义与合同 §7 的不一致：`export.revoke`/`device.revoke`/`node.revoke` 入口先读目标记录，**不存在或已撤销（`revokedAt` 非 null / `state = revoked`）→ `local.not_found`**，不得把底层 `COALESCE(revoked_at, ?)` 的幂等成功透出为成功（合同 §7：「`*.revoke` 重试得到 `local.not_found`」；已核实三处 SQL 均为 `COALESCE`）。读-写之间的竞态无害（最坏落到存储的幂等撤销）。写范围：`crates/server/src/local_admin/router.rs`（及必要的 `view.rs`/测试）。完成条件：三个 revoke 方法各有「重复撤销 → `local.not_found`」与「未知 id → `local.not_found`」的测试；`cargo test --locked -p server --all-features` 与 `npm run check` 全绿；日志追加 `reports/wp3-server-methods.log`。依据：WP3b2 开放项第 1 项与 Check Plan Changes 2026-09-25 条。

## 3. Branch Validation

- [x] 3.1 WP1；前置：2.3；实现 Agent（coder）。完成 WP1 交付前 project verify：`npm run verify`（[PV1]）与 `node scripts/check-crate-boundaries.mjs`（[PV2]），逐项记录完整命令、工具链版本、退出码与日志路径。完成条件：两项通过；资源已核实释放。
- [x] 3.2 WP1；前置：2.3；独立 reviewer。新建**不继承实现对话**的只读 reviewer 子 Agent，按 `roles/reviewer.md` 检视 WP1 固定版本（口径与 `design.md` D1/D3/D5 一致、依赖登记与 `deny.toml`、文档引用归属）。完成条件：`openspec/changes/daemon-cli-and-local-admin/reports/rv1-wp1.md` 记录 Agent ID、版本、隔离方式与结论；阻断项修复后由新的隔离子 Agent 复核。
- [x] 3.3 WP2；前置：2.6；实现 Agent（coder）。完成 WP2 交付前 project verify：wrapper 自身测试（Windows 本机 [PV5]）与 `npm run verify`（[PV1]，确认 workspace 不受影响）。完成条件：通过并留证；Windows 用例临时资源已清理。
- [x] 3.4 WP2；前置：2.6；独立 reviewer。新建隔离子 Agent 检视 WP2：unsafe 收敛范围与不变量注释、safe API 语义与错误分类、非 Windows 失败关闭、公开面不暴露原始句柄。完成条件：`openspec/changes/daemon-cli-and-local-admin/reports/rv1-wp2.md` 记录隔离方式、版本与结论；阻断项修复后复核。
- [ ] 3.5 WP3；前置：2.13；实现 Agent（coder）。完成 WP3 交付前 project verify：`cargo test --locked -p server --all-features`（[PV3]）、Windows 本机 `cfg(windows)` 用例（[PV5]）、`node scripts/check-crate-boundaries.mjs`（[PV2]）、`npm run verify`（[PV1]），并核对用例确实被执行（无 0 用例、无全跳过）。完成条件：逐项通过并留证（[PV5] 无法执行时如实记 BLOCKED）；临时数据目录已清理。
- [x] 3.6 WP3；前置：2.13；独立 reviewer。新建隔离子 Agent 检视 WP3：framing 关闭规则逐条、对端凭据校验不可绕过、信封 closed object 与 `v` 规则、方法路由只经 `core::use_cases`、凭据值不进入日志/错误/`Debug`、审计导出内容边界、无 unsafe。完成条件：`openspec/changes/daemon-cli-and-local-admin/reports/rv1-wp3.md` 记录隔离方式、版本与结论；阻断项修复后复核。
- [ ] 3.7 WP4；前置：2.18；实现 Agent（coder）。完成 WP4 交付前 project verify：`cargo test --locked -p app --all-features`（[PV4]）、Windows 本机 `cfg(windows)` 用例（[PV5]）、`npm run verify`（[PV1]），核对真实子进程集成测试确实执行。完成条件：逐项通过并留证；临时 Daemon 数据目录与锁文件已清理。
- [ ] 3.8 WP4；前置：2.18；独立 reviewer。新建隔离子 Agent 检视 WP4：CLI 零业务规则与零 SQLite 直连、退出码/stderr 契约、SAS/指纹逐字校验、关闭顺序与任务取消、秘密不进入 shell 可见位置/日志/文件。完成条件：`openspec/changes/daemon-cli-and-local-admin/reports/rv1-wp4.md` 记录隔离方式、版本与结论；阻断项修复后复核。
- [ ] 3.9 WP5；前置：2.20；实现 Agent（coder）。完成 WP5 交付前 project verify：`npm run verify`（[PV1]）与 `node scripts/check-crate-boundaries.mjs`（[PV2]，成员已加入后逐条核对 §5 的 `server`/`app` 行与列）。完成条件：两项在固定版本上通过。
- [ ] 3.10 WP5；前置：2.20；独立 reviewer。新建隔离子 Agent 检视 WP5 的文档改动：状态表与 `cargo metadata` 实际一致、未误写未落地 crate 为已落地、未误改切片顺序与验收表述。完成条件：`openspec/changes/daemon-cli-and-local-admin/reports/rv1-wp5.md` 记录隔离方式、版本与结论；阻断项修复后复核。

## 4. Test Design and Authoring

不适用：Main E2E mode 为 `not-applicable`（计划已含 2026-09-25 用户降级批准），不生成 TP 与用例设计任务；各 WP 的行为测试由实现 Agent 自带（2.x），最终替代验证见 7.1。

## 5. Integration Readiness

- [ ] 5.1 主 Agent（仅一次，不随交付单元复制）。单独创建独立集成 Agent，显式交接 `roles/integrator.md` 全文、本计划与相关契约、源提交及已验收证据、独立集成 worktree、目标分支 `refs/heads/main` 与授权边界（apply 已授权本地合入；推送远端、回滚、发布需另行授权）。完成条件：记录集成 Agent 的实际 ID、上下文方式与交接清单；主 Agent 不兼任集成执行者；宿主缺少独立执行能力时本任务与第 6 组合并入相关任务记 BLOCKED，并如实上报。
- [ ] 5.2 DU1；前置：3.1–3.10；主 Agent。复核 DU1 的预定模式（`integrated`）与组成（WP1–WP5），核对 [PV1]–[PV5] 与 RV1 的有效证据，确认无未解决的阻断项与未登记漂移；`integrated` 模式下引用组合后的 verify/review 结果，不重复单 WP 的检查。完成条件：计划与依赖已同步（无待更新项）、就绪判据全部满足；变化先同步计划与依赖再进入第 6 组。

## 6. Merge Unit

- [ ] 6.1 DU1；前置：5.2；主 Agent（机械核实可派发 environment/recon）。按计划核实目标仓库与主分支当前提交：`git -C D:\Project\acp-remote rev-parse refs/heads/main`、`git status --porcelain`、`git worktree list`，记录准确引用与核实证据。完成条件：基线提交被明确记录到 `verification.md`；无法确认目标时保持 BLOCKED，不凭 `HEAD` 或上次记录的引用继续。
- [ ] 6.2 DU1；前置：6.1；集成执行者。基于已核实基线构造 DU1 候选：确认变更分支包含 WP1–WP5 的全部提交、成员与依赖登记完整、`reports/` 与 `verification.md` 已登记；固定基线与候选版本并记录组成与构建结果（`cargo build --locked --workspace --all-features` + `vendor/windows-local-ipc` 构建）。完成条件：候选版本可固定（提交哈希）且组成可复述；基线变化时从 6.1 重开。
- [ ] 6.3 DU1；前置：6.2；检查执行者。按计划 Check ID 完成候选 Project Verify：[PV1]、[PV2]、[PV3]、[PV4]，并按平台条件执行 [PV5]（Linux CI 不覆盖该路径，需本地 Windows 执行）。完成条件：逐项记录版本、范围、退出码、日志路径，且无零用例/全跳过；有效复用旧证据时逐项写明适用性。
- [ ] 6.4 DU1；前置：6.2；独立 reviewer。只读检视固定候选的新增交互与冲突解决（文档口径 ↔ 实现、`Cargo.toml` 成员与依赖、`deny.toml` path 登记、CLI↔server 形状一致性），必要时复核 [PV2] 的差异结论；待返回的检查证据在证据交付前补齐核对。完成条件：`openspec/changes/daemon-cli-and-local-admin/reports/rv1-du1.md` 记录隔离设置、版本与结论；阻断项修复后由新的隔离子 Agent 复核。
- [ ] 6.5 DU1（`not-applicable` 路径）；前置：6.2；主 Agent。核对 Main E2E 的 `not-applicable` 理由与依据仍成立（无跨节点/跨客户端产品闭环、`x-agentic.e2e.command` 为空、2026-09-25 用户降级批准记录有效并已写入 `plan.md`），并确认候选阶段的替代检查覆盖（[PV3]/[PV4]/[PV5] 的候选轮次已完成、证据可读）。完成条件：四项（reason/basis/alternative_checks/downgrade_approval）与 `plan.md` 一致且未被实现期改动削弱；不在候选阶段重复第 7 组的最终替代验证。
- [ ] 6.6 DU1；前置：6.3、6.4、6.5；集成执行者。确认候选证据完整后按计划核对基线，以条件更新或串行合并机制防止竞态，把 DU1 合入本地主分支并记录实际提交；**不 push**（远端操作另需明确授权）。完成条件：主分支实际提交与候选一致性可核对；基线在候选中途变化时重开受影响的 6.1–6.5。
- [ ] 6.7 DU1；前置：6.6；检查执行者。核对实际主分支结果与候选一致，并完成计划内必需的主分支回归：[PV1]、[PV2]（以及按平台条件可行的 [PV3]/[PV4]/[PV5]）。完成条件：日志写入 `openspec/changes/daemon-cli-and-local-admin/reports/du1-main-verify.log`；逐项记录复用的旧证据与适用性判断；通过前不处理任何后续合入单元。
- [ ] 6.8 DU1；前置：6.6；独立 reviewer。检视合并到主分支后新增的差异（相对候选是否引入冲突解决或额外改动）。完成条件：无新增差异时由主 Agent 记录依据并引用 6.4 的 review ID；有新增差异时由隔离子 Agent 复核并留证；通过前不处理后续合入单元。

## 7. Final E2E

- [ ] 7.1 全变更（`not-applicable` 的替代验证）；前置：6.7；主 Agent（可派发独立执行者采集原始输出）。在最终主分支的固定版本上执行 `plan.md` 的 `alternative_checks`：`cargo test --locked -p server --all-features`（[PV3]）、`cargo test --locked -p app --all-features`（[PV4]）、Windows 本机 `cfg(windows)` 用例与 `vendor/windows-local-ipc` 自身测试（[PV5]）、`npm run check` 与 `npm run verify`（[PV1]/[PV2]）。完成条件：返回逐项原始结果（命令、版本、退出码、日志路径、跳过数与原因）；这是替代验证的**真实执行**，不以任何检查的 PASS 作为本任务的完成条件。
- [ ] 7.2 全变更；前置：7.1；主 Agent。汇总全部必要覆盖行的断言、版本与隔离证据，处理执行中出现的问题（失败按原 WP 重新派发并重跑受影响检查），核实运行资源（临时数据目录、worktree、`CARGO_TARGET_DIR`）已清理，并把 Main E2E 结论记为 `NOT_APPLICABLE`（附理由、依据与全部通过的替代验证）。完成条件：`verification.md` 中替代验证与覆盖索引逐行关联、无未闭环 FAIL/BLOCKED、无残留资源。
- [ ] 7.3 [e2e-owned] 全变更；前置：7.2；扩展（openspec-agentic）。运行 `npx --quiet --no-install openspec-agentic e2e check --change daemon-cli-and-local-admin`，确认「不适用判据已按计划固化」（降级批准可追溯、替代检查清单齐备）。完成条件：由该检查在 PASS 时自动勾选本行、非 PASS 时自动回退；主 Agent 不得手勾或手动回退。本行只检查门禁，不执行测试、不汇总结果。

## 8. Final Verification

- [ ] 8.1 [final-verification] 全变更；前置：7.3；主 Agent。使用 `.agents/skills/agentic-verify/SKILL.md` 执行最终验收，核对用户意图（`DEVELOPMENT_PLAN.md` 切片 4 原文与本会话授权）、需求覆盖、设计一致性、计划与任务完成度、最终主分支证据；记录当前 agentic-assessment 后运行 `npx --quiet --no-install openspec-agentic workflow check --change daemon-cli-and-local-admin --stage final --json`，全部通过才完成。
