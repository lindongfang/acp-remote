# PV3 / WP5 — 依赖方向与 core allow-list
date: 2026-09-23T08:52:19Z

$ node scripts/check-crate-boundaries.mjs
crate boundaries OK: 6 个 crate 的依赖方向与 §5 矩阵一致（6 个已在矩阵登记）
exit=0

# 说明：core 的直接依赖从 2 个（async-trait/thiserror）扩到 4 个（+p256/sha2）；
# allow-list 已按 cargo tree -p core --edges normal 的实际输出登记；§9 判据 13 / AGENTS.md §12 /
# MODULE_ARCHITECTURE.md §3.1 的文档同步仍属任务 2.2（未完成）。

## W0 复跑（合同并入与 v2 DDL 落地之后）

命令：`node scripts/check-crate-boundaries.mjs`（仓库根）

退出码：0

```text
crate boundaries OK: 6 个 crate 的依赖方向与 §5 矩阵一致（6 个已在矩阵登记）

```

说明：`core` 的直接依赖为 `async-trait`/`thiserror`/`p256`/`sha2`，普通依赖闭包与 `CORE_ALLOWED_CLOSURE` 逐项相等；该结论同时是合同 §9 判据 13 的落地证据。
