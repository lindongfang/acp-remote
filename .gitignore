# Rust workspace
/target/
**/*.rs.bk

# `vendor/windows-local-ipc` 不是 workspace 成员（根 `Cargo.toml` 的 `workspace.exclude`），
# 因此它可以（也需要）在自己的目录里独立 `cargo build/test`——那会在目录下生成 `target/` 与 `Cargo.lock`，
# 上面的 `/target/` 只匹配仓库根。两者都不入库：作为 path 依赖被 `server` 消费时，构建产物进根 `target/`、
# 版本解析由根 `Cargo.lock` 覆盖；独立的 `Cargo.lock` 只是本机独立构建的副产物。
/vendor/windows-local-ipc/target/
/vendor/windows-local-ipc/Cargo.lock

# Node 工具链（合同校验脚本的 devDependencies）
node_modules/
npm-debug.log*

# 前端构建产物
dist/

# 本地环境与日志
.env
.env.*
*.log
# 变更/归档记录里的**原始证据日志**（`openspec/changes/**/reports/**/*.log`）同样不入库：
# 它们只是工作区内的过程证据，归档记录（`verification.md` 的 Checks 表）已给出命令、环境与结果摘要，
# 复盘时在对应目标提交上重跑同样命令即可复现；不要把这类日志 `git add -f` 强行入库。
# 本行与上面的 `*.log` 重叠，显式写出是为了固定这条策略、避免后续被误当作「遗漏的证据」补提。
openspec/changes/**/reports/**/*.log

# 仓库根的 reports/ 是早期变更的历史遗留（当时证据直接写在仓库根）：
# 现行约定是 openspec/changes/<变更>/reports/（见上），仓库根不再接受证据/日志目录，
# 因此这份遗留目录整体不入库；本地文件保留，是否删除由开发者自行决定。
/reports/
